import { createContext } from "react";

/**
 * ProjectContext provides a context for managing the specific project selecting from the project list
 *
 * @returns {React.Context} A context object for accessing and updating project state within the component.
 */
export const ProjectContext = createContext();
