import { createContext } from "react";

/**
 * UserContext provides a context for managing the participant username
 *
 * @returns {React.Context} A context object for accessing and updating participant username
 */
export const UserContext = createContext();
